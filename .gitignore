# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# local agent token-tracking state (not part of the project)
.tokensave/

# maintainer-only working files (pulled feedback, service-account keys)
.local/

# local build-time config (e.g. the Firebase feedback endpoint) — never
# committed; copy .env.example to .env. A real env var overrides it, so CI
# (which injects repository secrets) is unaffected.
.env
