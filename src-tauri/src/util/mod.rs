//! Small, dependency-free utilities shared across services and feature
//! modules. Currently just [`time`]; add siblings here rather than
//! re-duplicating one-off helpers per module.

pub mod time;
