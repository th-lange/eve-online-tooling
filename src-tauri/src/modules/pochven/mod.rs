//! Pochven — entry/logistics helpers (epic #417). `candidates` holds the
//! curated C729 exit-system data (from Electus Matari); `commands` computes
//! live per-system routing to the trade hubs over the SDE stargate graph.

mod candidates;
pub mod commands;
