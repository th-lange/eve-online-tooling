/**
 * Shared UI copy strings reused across modules — keep terminology consistent
 * with the location glossary in CLAUDE.md (Region / System / Station / Hub).
 */

/** Tooltip for region/station pickers: clarifies which level of the location
 *  hierarchy prices vs. orders/assets are scoped to (#849). */
export const REGION_STATION_TOOLTIP =
  "Prices and history are regional; orders and assets are per-station.";
